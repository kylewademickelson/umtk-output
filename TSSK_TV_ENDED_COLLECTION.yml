collections:
  Ended Shows:
    summary: Shows that have ended
    item_label: EndedShows
    non_item_remove_label: EndedShows
    build_collection: false
    ignore_blank_results: true
    sync_mode: append
    plex_all: true
    filters:
      tmdb_status: ended
